// Importing User model
const User = require('./User');

// Importing Thought model
const Thought = require('./Thought');

// Exporting the imported models to be used in other parts of the application
module.exports = { User, Thought };
